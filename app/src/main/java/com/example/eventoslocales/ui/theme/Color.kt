package com.example.eventoslocales.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Archivo base de colores reutilizables.
 *
 * Aquí se definen tonos complementarios que pueden ser usados
 * en cualquier parte de la interfaz o dentro del tema principal.
 *
 * Decisión: mantener esta paleta separada facilita el mantenimiento
 * y la personalización de colores, sin tener que modificar el Theme completo.
 */

// Tonos claros — usados en modo oscuro o para resaltar áreas secundarias
val Purple80 = Color(0xFFD0BCFF)     // Violeta suave (ideal para fondos secundarios)
val PurpleGrey80 = Color(0xFFCCC2DC) // Gris violáceo claro
val Pink80 = Color(0xFFEFB8C8)       // Rosa pálido, buen acento suave

// Tonos oscuros — usados en modo claro o para contrastes de texto/iconos
val Purple40 = Color(0xFF6650A4)     // Violeta oscuro principal
val PurpleGrey40 = Color(0xFF625B71) // Gris violáceo medio, equilibrado
val Pink40 = Color(0xFF7D5260)       // Rosa oscuro, se usa para acentos o detalles
